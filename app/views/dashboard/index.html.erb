<div class="bg-gray-100 min-h-screen w-full">
  <%= render 'shared/header' %>

  <div class="px-4 py-8 max-w-7xl mx-auto">
    <!-- ウェルカムメッセージ -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-2">
        ようこそ、<%= current_user.name %>さん
      </h1>
      <p class="text-gray-600 dark:text-gray-400">
        参加したい卓を選んでグループを作成しましょう
      </p>
    </div>

    <!-- あなたの卓予定ボタン -->
    <div class="mb-8">
      <button class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition-colors duration-200 shadow-md">
        あなたの卓予定
      </button>
    </div>

    <!-- 卓一覧表示エリア -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <!-- サンプル卓カード（身内卓） -->
      <%= render 'shared/table_card',
        table_name: '卓1',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: '卓2',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Bさん卓',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Aさん卓',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Dさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Bさん卓',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>
    </div>
  </div>
</div>
