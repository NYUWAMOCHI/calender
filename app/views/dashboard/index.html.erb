<% content_for :head do %>
  <style>
    main { max-width: none !important; margin: 0 !important; padding: 0 !important; }
    body { margin: 0 !important; padding: 0 !important; }
  </style>
<% end %>

<div class="min-h-screen bg-[#0D0D0D] flex flex-col">
  <%= render 'shared/header' %>

  <div class="flex-1 w-screen overflow-y-auto">
    <div class="w-screen p-6">
      <!-- あなたの卓予定ボタン -->
      <div class="mb-8">
        <button class="border-2 border-white text-white px-8 py-3 rounded-lg hover:bg-white hover:text-black transition-colors duration-200">
          あなたの卓予定
        </button>
      </div>

      <!-- 卓一覧表示エリア -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <!-- サンプル卓カード（身内卓） -->
      <%= render 'shared/table_card',
        table_name: '卓1',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: '卓2',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Bさん卓',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Aさん卓',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Dさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Bさん卓',
        is_private: true,
        time: '21時〜24時の卓です' %>

      <%= render 'shared/table_card',
        table_name: 'Cさん卓',
        is_private: false,
        time: '21時〜24時の卓です' %>
      </div>
    </div>
  </div>
</div>
